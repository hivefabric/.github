# Control Plane UI (`hive-control-plane-ui`)

## Role

Operator-facing dashboard for cluster and task observability.

## Responsibilities

- node inventory and telemetry visibility
- task lifecycle and assignment visibility
- runtime capability and freshness indicators

## Local run

```bash
cd hive-control-plane-ui
npm install
npm run dev
```

Default development port is typically `5175`.
